extends layout

block contenido 
        //- h2 Mi nombre es Juan - Layout
        h1 #{mensaje} #{nombre} #{apellido}
        if render 
            h2 El deporte favorito es #{deporte}
        else 
            h2 El deporte mas aburrido es #{deporte}
